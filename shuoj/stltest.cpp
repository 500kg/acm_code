#include<set>
#include<iostream>
using namespace std;
int main()
{
    set<int> ST={1,2,4,3,3};
    for(int x:ST)
        cout<<x<<endl;
    ST.insert(8);
    ST.insert(-1);    //log n;
    cout<<endl;
    for(int x:ST)
        cout<<x<<endl;
        bool operator <(const par & rhs) const
        {
            key<rhs.key;
        }
}
